export type Profile = Float32Array;

export type InitConc = { xr0: number, xw0: number };

export type MagnifierParticleInfo = {
    num: number,
    fracR: number,
    rVel: number,
    wVel: number
};