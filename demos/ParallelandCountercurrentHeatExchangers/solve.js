function solve(args) {
    hot_temperatures = [400, 400, 400, 400];
    cold_temperatures = [300, 300, 300, 300];

    for(let rounds = 0; rounds < 100; rounds++) {
        for(let i = 0; i < hot_temperatures.length; i++) {
            // calculate heat transfer
            // use eulers to find temperature of next point
            // repeat
        }
    }
}